<section class="section">
  <div class="container">
    <div class="columns">
      <% if user_signed_in? %>
        <%= render 'profile', user: @user %>
      <% else %>
        <%= render 'trends' %>
      <% end %>
      <%= render 'feed', user: @user %>
      <%= render 'who-to-follow' %>
    </div>
  </div>
</section>
