<section class="section">
  <div class="container">
  	<div class="columns is-centered">
  	  <div class="column is-6">
        <p class="font-weight-bold">フォロー中</p><br>
        <% if @user.following_user.exists? %>
          <%= render 'follow_user', users: @user.following_user %>
        <% else %>
          <p class="text-center is-size-4">フォローしているユーザーはいません</p>
        <% end %>
        <br>
        <%= link_to 'Back', 'javascript:history.back()', class: "button is-warning is-light" %>
      </div>
    </div>
  </div>
</section>